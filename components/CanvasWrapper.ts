import { styled } from 'styles/stitches.config'

const CanvasWrapper = styled('div', {
  position: 'fixed',
  top: 0,
  left: 0,
  width: '100vw',
  height: '100vh',
  background: '#000000',
})

export default CanvasWrapper
